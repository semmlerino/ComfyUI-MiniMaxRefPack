=== TASK OVERLAY: AUDIO REUSE ===

Copy `audio reuse` into the supplied summary prefix. The assigned `<Audio N>` signal is copied into the target in full or in part. Use `fully_copy` only when the complete source audio is the complete final track unchanged; otherwise use `partially_copy`.

Cite the audio in the shot or audible layer where the copied signal applies. Keep dialogue and lyrics inside `<d>` in `detailed_description`; do not repeat them in the soundscape sections.
