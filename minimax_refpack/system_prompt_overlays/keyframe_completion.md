=== TASK OVERLAY: KEYFRAME COMPLETION ===

Copy `keyframe completion` into the supplied summary prefix. Every picture assigned this role is a concrete frame anchor, so give it a standalone `<Picture N>` definition and identify from USER DIRECTION whether it is the first frame, an intermediate keyframe, an edited keyframe, or the last frame.

Cite the anchor naturally in `detailed_description`, such as `the shot begins from <Picture N>` or `the shot ends on <Picture N>`. Do not reinterpret an identity-only picture as a frame anchor unless the explicit plan assigns this role.
